<view class="todo-page">
  <!-- 背景装饰 -->
  <view class="bg-decoration bg-decoration--1"></view>
  <view class="bg-decoration bg-decoration--2"></view>

  <view class="content-wrapper">
    <!-- 顶部统计卡片 -->
    <view class="glass-card hero-card">
      <view class="hero-header">
        <view class="hero-title">待办清单</view>
        <view class="hero-date">{{todayDate}}</view>
      </view>
      <view class="hero-stats">
        <view class="stat-item">
          <text class="stat-num">{{activeCount}}</text>
          <text class="stat-label">待处理</text>
        </view>
        <view class="stat-divider"></view>
        <view class="stat-item">
          <text class="stat-num">{{completedCount}}</text>
          <text class="stat-label">已完成</text>
        </view>
      </view>
      <view class="progress-bar">
        <view class="progress-bar__fill" style="width: {{progressPercent}}%"></view>
      </view>
    </view>

    <!-- 输入区域 -->
    <view class="glass-card input-card">
      <input
        class="todo-input"
        placeholder="添加新的待办事项..."
        placeholder-class="todo-input__placeholder"
        value="{{inputValue}}"
        confirm-type="done"
        bindinput="onInputChange"
        bindconfirm="onAddTask"
      />
      <view class="add-btn-wrapper" bindtap="onAddTask" style="opacity: {{canSubmit ? 1 : 0.5}}">
        <view class="add-icon"></view>
      </view>
    </view>

    <!-- 过滤器 -->
    <view class="filter-bar">
      <view
        class="filter-item {{filter==='all'?'filter-item--active':''}}"
        data-filter="all"
        bindtap="onFilterChange"
      >全部</view>
      <view
        class="filter-item {{filter==='active'?'filter-item--active':''}}"
        data-filter="active"
        bindtap="onFilterChange"
      >进行中</view>
      <view
        class="filter-item {{filter==='completed'?'filter-item--active':''}}"
        data-filter="completed"
        bindtap="onFilterChange"
      >已完成</view>
    </view>

    <!-- 任务列表 -->
    <scroll-view scroll-y class="todo-list" wx:if="{{filteredTasks.length}}" show-scrollbar="{{false}}" enhanced="{{true}}">
      <block wx:for="{{filteredTasks}}" wx:key="id">
        <view class="todo-item {{item.completed?'todo-item--done':''}}" bindtap="onToggleTask" data-id="{{item.id}}">
          <view class="checkbox {{item.completed?'checkbox--checked':''}}">
            <view class="checkmark" wx:if="{{item.completed}}"></view>
          </view>
          <view class="todo-content">
            <view class="todo-title">{{item.title}}</view>
            <view class="todo-time" wx:if="{{item._createdText}}">{{item._createdText}}</view>
          </view>
          <view class="todo-actions">
            <view class="action-btn edit-btn" catchtap="onEditTask" data-id="{{item.id}}">
              <view class="icon-edit"></view>
            </view>
            <view class="action-btn delete-btn" catchtap="onDeleteTask" data-id="{{item.id}}">
              <view class="icon-delete"></view>
            </view>
          </view>
        </view>
      </block>
      
      <view class="clear-btn-wrapper" wx:if="{{completedCount > 0 && filter !== 'active'}}">
        <view class="clear-btn" bindtap="onClearCompleted">清除已完成任务</view>
      </view>
    </scroll-view>

    <!-- 空状态 -->
    <view class="empty-state" wx:else>
      <view class="empty-icon-glass"></view>
      <view class="empty-text">暂无任务，享受当下</view>
    </view>
  </view>

  <!-- 编辑弹窗 -->
  <view class="glass-modal-mask {{showEditor?'visible':''}}" bindtap="onCloseEditor">
    <view class="glass-modal {{showEditor?'visible':''}}" catchtap="stopProp">
      <view class="modal-header">
        <text>{{editingTaskId?'编辑任务':'新建任务'}}</text>
      </view>
      <textarea
        class="modal-input"
        value="{{editorValue}}"
        placeholder="输入任务内容..."
        placeholder-class="modal-input__placeholder"
        auto-height
        bindinput="onEditorInput"
      />
      <view class="modal-footer">
        <view class="modal-btn cancel-btn" bindtap="onCloseEditor">取消</view>
        <view class="modal-btn confirm-btn {{!editorCanSubmit?'disabled':''}}" bindtap="onSubmitEditor">保存</view>
      </view>
    </view>
  </view>
</view>

